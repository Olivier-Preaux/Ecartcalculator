<form method="post" action="{{ path('ecart_delete', {'id': ecart.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ecart.id) }}">
    <button class="btn">Delete</button>
</form>
